/* This page is full-bleed and dark; the global header is hidden. */
export default function MapLayout({ children }: { children: React.ReactNode }) {
  return <div className="map-page">{children}</div>;
}
